const renderClassTable = require('./renderClassTable');
const renderFunctionTable = require('./renderFunctionTable');

module.exports = {
    classTable: renderClassTable,
    functionTable: renderFunctionTable
};
