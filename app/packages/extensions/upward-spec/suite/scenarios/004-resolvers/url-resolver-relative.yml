status: 200
headers:
  inline:
    content-type: text/plain
body: rewrittenUrl

rewrittenUrl:
  baseUrl: documentBase
  pathname: request.url.query.id

documentBase:
  baseUrl: rootUrl
  pathname:
    inline: document/

rootUrl:
  baseUrl: false
  pathname:
    inline: scope/
