machine:
  node:
    version: 8
dependencies:
  override:
    - cd theme && npm i
test:
  override:
    - cd theme && npm t
checkout:
  post:
    - cd ../ && git clone git@github.com:magento-research/peregrine.git
