{
    "projects": {
        "venia": {
            "schemaPath": "lastCachedSchema.json",
            "extensions": {
                "endpoints": {
                    "default": "${env:MAGENTO_BACKEND_URL}graphql"
                }
            }
        }
    }
}
