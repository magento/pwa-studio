import PropTypes from 'prop-types';

export const PURCHASE_HISTORY_ITEM_PROP_TYPES = {
    id: PropTypes.number,
    imageSrc: PropTypes.string,
    title: PropTypes.string,
    date: PropTypes.date,
    link: PropTypes.string
};
