/**
 * Groups of URL query strings which should persist in scenarios
 * when new query string is being pushed. The case below covers persistence
 * of query strings on category page and search results page.
 */
export const persistentQueries = ['page', 'query', 'category'];
