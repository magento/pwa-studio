query getCmsPage($id: Int!) {
    cmsPage(id: $id) {
        url_key
        content
        content_heading
        title
        page_layout
        meta_title
        meta_keywords
        meta_description
    }
}
