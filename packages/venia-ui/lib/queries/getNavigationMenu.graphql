query navigationMenu($id: Int!) {
    category(id: $id) {
        id
        name
        url_suffix
        children {
            children_count
            id
            include_in_menu
            name
            position
            url_path
        }
    }
}
