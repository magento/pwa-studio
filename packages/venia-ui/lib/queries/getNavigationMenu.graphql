query navigationMenu($id: Int!) {
    storeConfig {
        id
        category_url_suffix
    }
    category(id: $id) {
        id
        name
        children {
            children_count
            id
            include_in_menu
            name
            position
            url_path
        }
    }
}
